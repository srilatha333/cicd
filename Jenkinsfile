pipeline {
  environment {
    ENVRMNT = "pdc"
  }
  agent any 
  stages {
    stage('Cloning Git') {
      steps {
        sh '''echo hostname
            echo "Hello-World"'''
         }
      }
    }
  }
